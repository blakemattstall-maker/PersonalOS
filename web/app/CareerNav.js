// The switcher stays as a component so Jobs and Pipeline can be restored in
// one place. With Money as the only active career surface, no switcher is
// shown.
export default function CareerNav() {
  return null;
}
